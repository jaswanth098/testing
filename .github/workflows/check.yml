name: machine spawn

on: [push, pull_request]

jobs:
  wait:
    runs-on: [self-hosted]
    name: testing
    steps:
    - uses: actions/checkout@v1
    - name: Building image
      run: |
          sleep 2m
          echo "testing complete"
