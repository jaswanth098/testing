name: machine spawn

on: [push, pull_request]

jobs:
  wait:
    runs-on: [self-hosted]
    name: testing
    steps:
    - uses: actions/checkout@v1
    - name: Building image
      run: |
          tail -f /var/log/dpkg.log  > /dev/null
          echo "testing complete"
